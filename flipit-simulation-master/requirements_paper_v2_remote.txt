# Minimal dependency set for the current V2 paper pipeline on a remote machine.
# PyTorch is installed separately by paper_v2_remote_setup.ps1 because CPU/GPU
# wheels often need a specific index URL.

numpy>=1.24,<3
matplotlib>=3.7,<4
PyYAML>=6,<7
gymnasium==0.29.1
gym==0.26.2
pytest>=8,<9
